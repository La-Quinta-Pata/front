

export default function Hero() {

  return (

    <section className="w-full bg-gray-200/70 text-center py-6 px-4 flex flex-col items-center justify-center"
      role="region"
      aria-labelledby="hero-heading">
      <h1
        id="hero-heading"
        className="text-4xl lg:text-6xl font-extrabold tracking-tight"
      >
        <span className="text-gray-900">La Quinta Pata</span>
      </h1>

    </section>

  );
}
